apply plugin: "java"
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
sourceSets.main.java.srcDirs = ["src/"]
buildscript {
    repositories {
        mavenCentral()
    }

    repositories {
        mavenCentral()
    }
}

dependencies {
}
